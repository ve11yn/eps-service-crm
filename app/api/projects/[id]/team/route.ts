import { NextResponse } from "next/server";
import { routeErrorResponse } from "@/backend/observability/errors";
import { removeProjectTeamMember, saveProjectTeamMember } from "@/backend/services/projects/project-management";
import { requireApiSession } from "@/lib/auth/api";
type Context = { params: Promise<{ id: string }> };
export async function POST(request: Request, { params }: Context) { const auth = await requireApiSession(["owner", "admin", "coordinator"]); if (!auth.ok) return auth.response; try { const { id } = await params; const body = await request.json() as { profileId?: string; teamRole?: string; isLead?: boolean }; if (!body.profileId) return NextResponse.json({ success: false, error: "profileId is required" }, { status: 400 }); return NextResponse.json({ success: true, member: await saveProjectTeamMember(id, body.profileId, body.teamRole ?? "worker", Boolean(body.isLead), auth.session.profile.id) }); } catch (error) { return routeErrorResponse({ scope: "api.project_team.save", error, details: { performedByProfileId: auth.session.profile.id } }); } }
export async function DELETE(request: Request, { params }: Context) { const auth = await requireApiSession(["owner", "admin", "coordinator"]); if (!auth.ok) return auth.response; try { const { id } = await params; const body = await request.json() as { profileId?: string }; if (!body.profileId) return NextResponse.json({ success: false, error: "profileId is required" }, { status: 400 }); await removeProjectTeamMember(id, body.profileId, auth.session.profile.id); return NextResponse.json({ success: true }); } catch (error) { return routeErrorResponse({ scope: "api.project_team.remove", error, details: { performedByProfileId: auth.session.profile.id } }); } }
