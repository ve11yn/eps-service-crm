"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export function CustomerForm({customer}:{customer?:{id:string;full_name:string;primary_phone:string|null;whatsapp_number:string|null;email:string|null;notes:string|null;is_archived:boolean}}){const router=useRouter();const[name,setName]=useState(customer?.full_name??"");const[phone,setPhone]=useState(customer?.primary_phone??"");const[whatsapp,setWhatsapp]=useState(customer?.whatsapp_number??"");const[email,setEmail]=useState(customer?.email??"");const[notes,setNotes]=useState(customer?.notes??"");const[message,setMessage]=useState<string|null>(null);async function save(archive=customer?.is_archived??false){const response=await fetch(customer?`/api/customers/${customer.id}`:"/api/customers",{method:customer?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({fullName:name,primaryPhone:phone,whatsappNumber:whatsapp,email,notes,archive})});const payload=await response.json();if(!response.ok||!payload.success){setMessage(payload.error??"Save failed.");return}if(!customer)router.push(`/customers/${payload.customer.id}`);else router.refresh()}return <div className="form-grid"><label className="field-block"><span className="field-label">Name</span><input className="input" value={name} onChange={e=>setName(e.target.value)}/></label><label className="field-block"><span className="field-label">Phone</span><input className="input" value={phone} onChange={e=>setPhone(e.target.value)}/></label><label className="field-block"><span className="field-label">WhatsApp</span><input className="input" value={whatsapp} onChange={e=>setWhatsapp(e.target.value)}/></label><label className="field-block"><span className="field-label">Email</span><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="field-block"><span className="field-label">Notes</span><textarea className="input" value={notes} onChange={e=>setNotes(e.target.value)}/></label><div className="inline-actions"><button className="button button-primary" type="button" onClick={()=>save()}>Save customer</button>{customer?<button className="button button-secondary" type="button" onClick={()=>save(!customer.is_archived)}>{customer.is_archived?"Restore":"Archive"}</button>:null}</div>{message?<span className="helper-text">{message}</span>:null}</div>}
export function PropertyForm({property}:{property?:{id:string;property_name:string|null;address_line_1:string;address_line_2:string|null;unit_no:string|null;postal_code:string|null;access_notes:string|null;landmark_notes:string|null;is_archived:boolean}}){const router=useRouter();const[name,setName]=useState(property?.property_name??"");const[address,setAddress]=useState(property?.address_line_1??"");const[address2,setAddress2]=useState(property?.address_line_2??"");const[unit,setUnit]=useState(property?.unit_no??"");const[postal,setPostal]=useState(property?.postal_code??"");const[access,setAccess]=useState(property?.access_notes??"");const[landmark,setLandmark]=useState(property?.landmark_notes??"");const[message,setMessage]=useState<string|null>(null);async function save(archive=property?.is_archived??false){const response=await fetch(property?`/api/properties/${property.id}`:"/api/properties",{method:property?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({propertyName:name,addressLine1:address,addressLine2:address2,unitNo:unit,postalCode:postal,accessNotes:access,landmarkNotes:landmark,archive})});const payload=await response.json();if(!response.ok||!payload.success){setMessage(payload.error??"Save failed.");return}if(!property)router.push(`/properties/${payload.property.id}`);else router.refresh()}return <div className="form-grid"><label className="field-block"><span className="field-label">Property name</span><input className="input" value={name} onChange={e=>setName(e.target.value)}/></label><label className="field-block"><span className="field-label">Address</span><input className="input" value={address} onChange={e=>setAddress(e.target.value)}/></label><label className="field-block"><span className="field-label">Address line 2</span><input className="input" value={address2} onChange={e=>setAddress2(e.target.value)}/></label><label className="field-block"><span className="field-label">Unit</span><input className="input" value={unit} onChange={e=>setUnit(e.target.value)}/></label><label className="field-block"><span className="field-label">Postal code</span><input className="input" value={postal} onChange={e=>setPostal(e.target.value)}/></label><label className="field-block"><span className="field-label">Access instructions</span><textarea className="input" value={access} onChange={e=>setAccess(e.target.value)}/></label><label className="field-block"><span className="field-label">Landmark / property notes</span><textarea className="input" value={landmark} onChange={e=>setLandmark(e.target.value)}/></label><div className="inline-actions"><button className="button button-primary" type="button" onClick={()=>save()}>Save property</button>{property?<button className="button button-secondary" type="button" onClick={()=>save(!property.is_archived)}>{property.is_archived?"Restore":"Archive"}</button>:null}</div>{message?<span className="helper-text">{message}</span>:null}</div>}
