"use client";import{useState}from"react";import{useRouter}from"next/navigation";export function QuickBooksSync({invoiceId,syncId}:{invoiceId:string;syncId:string|null}){const router=useRouter();const[message,setMessage]=useState<string|null>(null);async function sync(){const r=await fetch(`/api/invoices/${invoiceId}/quickbooks`,{method:"POST"});const p=await r.json();if(!r.ok||!p.success){setMessage(p.error??"QuickBooks sync failed.");return}router.refresh()}return <div className="inline-actions">{syncId?<span className="helper-text">QuickBooks ID: {syncId}</span>:<button className="button button-secondary" onClick={sync}>Sync QuickBooks</button>}{message?<span className="helper-text">{message}</span>:null}</div>}
